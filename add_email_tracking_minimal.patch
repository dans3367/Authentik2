This patch adds minimal email tracking to the Go backend.
Apply manually by:

1. Add EmailContext struct after EmailSendResult (around line 52):

```go
// EmailContext contains metadata for tracking outgoing emails
type EmailContext struct {
	TenantID     string
	ContactID    *string
	EmailType    string
	NewsletterID *string
	CampaignID   *string
	PromotionID  *string
	Metadata     map[string]interface{}
}
```

2. The recordOutgoingEmail function and sendViaResend updates require careful manual integration.
   For now, the infrastructure is in place:
   - Database table created (migration 019)
   - Go models added
   - Repository methods added
   
3. To complete:
   - When calling email send functions, create an EmailContext with tenant_id and email_type
   - Pass it through the call chain to sendViaResend
   - Call recordOutgoingEmail after successful send

Example for future integration:

```go
emailCtx := &EmailContext{
    TenantID:  workflowInput.TenantID,
    EmailType: "birthday_card",
    ContactID: &workflowInput.ContactID,
}
result := sendViaResend(ctx, content, emailCtx)
```
